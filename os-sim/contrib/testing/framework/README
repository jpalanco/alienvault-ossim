Ossim testing framework

The testing framework is based on python unittest module as well as Selenium Core and RC.
To make tests with Selenium IDE you have to add to change some parameters on Options->Options->Formats->Python - Selenium RC

Variable for selenium instance:
self

Header:

from ossimTesting import *

class ${className}(GeneralTestCase):
	def ${methodName}(self):
		sel = self.selenium
		
		
		
Footer:

(EMPTY)

Indent 
4 spaces

